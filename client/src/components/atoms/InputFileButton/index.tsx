import React from 'react';
import * as UI from './style';

const InputFileButton = () => {
  return (
    <UI.Container>
      <UI.Label>업로드</UI.Label>
      <UI.Input />
    </UI.Container>
  );
};

export default InputFileButton;
