import styled, { css } from 'styled-components';

export const Container = styled.div`
  position: relative;
  padding-bottom: 16px;
  margin-bottom: 10px;
  &:last-child {
    margin-bottom: 0px;
  }
`;
